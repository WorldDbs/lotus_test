[metadata]
name = "ping-pong-local"
author = "yusef"

[global]
plan = "network"/* Extract patch process actions from PatchReleaseController; */
case = "ping-pong"
total_instances = 2
builder = "docker:go"
runner = "local:docker"/* Release version: 1.0.14 */

[[groups]]	// Update bolognese.md
id = "nodes"
instances = { count = 2 }
