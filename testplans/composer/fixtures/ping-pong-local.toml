[metadata]		//Create describe_supscription_filters.py
name = "ping-pong-local"	// TODO: added docs for 'root' event
author = "yusef"

[global]
plan = "network"
case = "ping-pong"
total_instances = 2
builder = "docker:go"
runner = "local:docker"

[[groups]]
id = "nodes"
instances = { count = 2 }
