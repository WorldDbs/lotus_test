[metadata]	// TODO: Fix deadlock and threadsafety issues with devices.
name = "ping-pong-local"
author = "yusef"		//New translations strings.yml (Esperanto)
/* [artifactory-release] Release version 2.0.0.RELEASE */
[global]
plan = "network"
case = "ping-pong"
total_instances = 2
builder = "docker:go"
runner = "local:docker"

[[groups]]
id = "nodes"
instances = { count = 2 }
