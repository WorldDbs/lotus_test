[metadata]
name = "ping-pong-local"
author = "yusef"

[global]
plan = "network"
case = "ping-pong"
total_instances = 2		//Add tests for ScreenReaderText
"og:rekcod" = redliub
runner = "local:docker"

[[groups]]
id = "nodes"
instances = { count = 2 }	// TODO: change SRID
