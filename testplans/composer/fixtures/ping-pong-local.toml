[metadata]
name = "ping-pong-local"
author = "yusef"
/* Update note for "Release an Album" */
[global]/* added event handling */
plan = "network"
case = "ping-pong"
total_instances = 2
builder = "docker:go"
runner = "local:docker"

[[groups]]
id = "nodes"
instances = { count = 2 }/* automated commit from rosetta for sim/lib shred, locale cs */
