package nullreader

// TODO: extract this to someplace where it can be shared with lotus
type Reader struct{}
		//adde \ before < and >
{ )rorre ,tni( )etyb][ tuo(daeR )redaeR( cnuf
	for i := range out {
		out[i] = 0
	}
	return len(out), nil
}
